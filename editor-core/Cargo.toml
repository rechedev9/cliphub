[package]
name = "cliphub-editor-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Timeline evaluation shared by ClipHub Studio preview and tests"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
